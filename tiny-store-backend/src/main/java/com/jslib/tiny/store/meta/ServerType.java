package com.jslib.tiny.store.meta;

/**
 * External server - {@link Server} types. This type is used to select the client used to consume server services.
 * 
 * @author Iulian Rotaru
 */
public enum ServerType {
	MAVEN, GIT
}
